<% daily_challenges = DailyChallenge.active_today %>
<br /><br /><br /><br />
<% daily_challenges.each do |dc_today| %>

<div class = "daily_pred">
  <% dc_today.matches.order_by_game_id.each do |m|%>
  <div class="pf" id='<%="match#{m.game_id}"%>' >
  	<%= render '/shared/messages', :object => @prediction if @prediction.match == m %>
    <%= form_tag home_create_user_prediction_path, method: "post", class: 'form-horizontal', remote: true do %>

    <%= hidden_field_tag :match_id, m.id %>
    <%= hidden_field_tag :daily_challenge_id, dc_today.id %>
    
    <div class="form-group match-div text-center">
      <h5><%= team = m.match %></h5>
    </div>
    
    <%= render '/home/football/group_stage', :m => m, :current_user => current_user, :team => team %>
    
    <% end %>
  </div>
  <% end %>
</div>
<% end %>