<% if last_challenge && last_challenge.has_predictions? %>
  <h5 class="padding-5 text-center">
  	<%= "#{last_challenge.name} winner is #{last_challenge.winner_array[0].strip.upcase} with #{last_challenge.winner_array[1]} points"  %>
  </h5>
<% end %>
